# Мой Проект

Нативный HTML проект с использованием Vite для сборки.

## 🚀 Быстрый старт

```bash
# Установка зависимостей
npm install

# Запуск сервера разработки
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр продакшен сборки
npm run preview
```

## 📁 Структура проекта

```
project/
├── src/
│   ├── styles/          # CSS файлы
│   ├── js/              # JavaScript модули
│   ├── components/      # HTML шаблоны
│   └── assets/          # Изображения, шрифты, иконки
├── pages/               # Дополнительные страницы
├── public/              # Статические файлы
└── index.html           # Главная страница
```

## 🛠 Технологии

- **Vite** — быстрый инструмент сборки
- **Нативный HTML/CSS/JS** — без фреймворков
- **ES Modules** — модульная архитектура JavaScript

## 📝 Команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск сервера разработки на порту 3000 |
| `npm run build` | Сборка оптимизированной версии в `dist/` |
| `npm run preview` | Локальный предпросмотр продакшен сборки |

## 📄 Лицензия

ISC
